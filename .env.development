# Local dev only: the Vite dev server does not serve /ws, so point the
# client at a separately running socket server (`bun run server`).
# Production builds leave VITE_WS_URL unset and use same-origin /ws.
VITE_WS_URL=ws://localhost:3001/ws
